'use client';

// const oldFetch = window.fetch;

// console.log('929299292');
// window.fetch = async (...arg) => {
//   console.log('fetch------');
//   const res = await oldFetch(...arg).then((res) => res.json());

//   if ([302, 303, 307, 308].includes(res.code)) {
//     window.location.href = res.url ?? '/login';
//   }

//   return res;
// };

export default function Common() {
  return <></>;
}
